package main

import (
	"fmt"

	"github.com/davidzr/xvla/compiler"
)

func main() {
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	compiler.NextToken()
	fmt.Println("Hello World")
}
